#ifndef PMERGEME_HPP
#define PMERGEME_HPP

#include <iostream>
#include <sstream>
#include <ctime>
#include <algorithm>
#include <vector>
#include <deque>

std::vector<int> fjSort(const std::vector<int> &arr);
std::deque<int> fjSort_2(const std::deque<int> &arr);

#endif